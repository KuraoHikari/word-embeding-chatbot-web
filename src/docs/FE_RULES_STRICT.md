Never use useEffect without cleanup.
Never mutate Zustand state directly.
Always use functional state updates.
Prevent duplicate messages by checking id existence.
Add memoization to ConversationItem using React.memo.
Use shallow selector from Zustand.
Prevent unnecessary re-renders.
